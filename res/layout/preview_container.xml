<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    android:id="@id/preview_container"
    android:clipChildren="false"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardCornerRadius="@dimen/media_corner_radius"
    app:cardElevation="0.0dip"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ViewStub
        android:id="@id/hydra_audio_indicator"
        android:layout="@layout/ps__hydra_focused_audio_chrome"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />
    <tv.periscope.android.graphics.GLRenderView
        android:id="@id/camera_playback"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:soundEffectsEnabled="false" />
</androidx.cardview.widget.CardView>