<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:icon="?toolbarOverflowIcon"
        android:id="@id/menu_more_options_bookmark_folders"
        android:visible="false"
        android:title="@string/more_options"
        app:showAsAction="ifRoom" />
    <item
        android:enabled="false"
        android:id="@id/menu_bookmark_edit_save"
        android:visible="false"
        android:title="@string/save"
        app:showAsAction="ifRoom" />
    <item
        android:id="@id/menu_clear_all_bookmarks"
        android:visible="false"
        android:title="@string/clear_all_bookmarks"
        app:showAsAction="never" />
</menu>